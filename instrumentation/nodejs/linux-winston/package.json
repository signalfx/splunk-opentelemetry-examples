{
  "name": "nodejs-linux-otel-example",
  "version": "1.0.0",
  "description": "This example demonstrates how to instrument a Node.js Application using OpenTelemetry, and export traces, metrics, and logs to a local collector, which will then export that data to Splunk. We'll use Node.js 18 for this example, but the steps for earlier Java versions are similar.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@splunk/otel": "^2.16.0",
    "@opentelemetry/api": "^1.9.0",
    "express": "^4.21.1",
    "winston": "^3.8.2"
  }
}
