name: go-aws-lambda

on:
  push:
    branches:
      - main
    paths:
      - "instrumentation/go/aws-lambda/**"
      - ".github/workflows/go-aws-lambda.yml"
  pull_request:
    paths:
      - "instrumentation/go/aws-lambda/**"
      - ".github/workflows/go-aws-lambda.yml"

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: instrumentation/go/aws-lambda/hello-world

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: instrumentation/go/aws-lambda/hello-world/go.mod
          cache-dependency-path: instrumentation/go/aws-lambda/hello-world/go.sum

      - name: Download modules
        run: go mod download

      - name: Build application
        run: go build -v .

      - name: Run go vet
        run: go vet ./...
